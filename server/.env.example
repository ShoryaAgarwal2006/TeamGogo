# ═══════════════════════════════════════════
# CivicPulse — Environment Variables
# Copy to .env and fill in your values
# ═══════════════════════════════════════════

# PostgreSQL connection (PostGIS-enabled database)
DATABASE_URL=postgresql://localhost:5432/civicpulse

# Server port (frontend is served from same origin)
PORT=3000

# Upload directory for report images
UPLOAD_DIR=./uploads
